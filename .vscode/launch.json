
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "gdb_test", 
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/XWebServer", // 可执行文件
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}", // program 的上一级目录
            "environment": [],
            "externalConsole": false, 
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask":  "build"    
        }
    ]
  }
  